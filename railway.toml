# Railway configuration — combined frontend + backend in one container.

[build]
builder = "dockerfile"
dockerfilePath = "docker/Dockerfile.combined"

[deploy]
healthcheckPath = "/api/papers"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
